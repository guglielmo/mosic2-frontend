export class Uffici {
    id: number;             // INT(10)
    codice: string;         // VARCHAR(255)
    codice_direzione: string;         // VARCHAR(255)
    denominazione: string;  // VARCHAR(255)
    disattivo_ufficio: string;
    ordine_ufficio: string;
    solo_delibere: string;
}